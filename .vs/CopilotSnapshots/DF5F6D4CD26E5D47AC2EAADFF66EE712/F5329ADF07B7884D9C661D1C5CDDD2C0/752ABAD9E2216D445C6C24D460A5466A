import { ChangeDetectionStrategy, Component, OnInit, inject } from '@angular/core';
import { CtaButtonComponent } from '../../shared/ui/cta-button.component';
import { LandingContentService } from '../../core/services/landing-content.service';
import { SeoService } from '../../core/services/seo.service';

@Component({
  selector: 'app-book-page',
  standalone: true,
  imports: [CtaButtonComponent],
  template: `
    <main class="page">
      <div class="page__inner">
        <h1>Book a discovery call</h1>
        <p>
          Use the calendar link below to schedule a call and discuss your automation goals.
        </p>
        <app-cta-button
          label="Open calendar"
          [link]="bookingUrl"
          [external]="true"
        ></app-cta-button>
      </div>
    </main>
  `,
  changeDetection: ChangeDetectionStrategy.OnPush,
  styles: [
    `
      .page {
        min-height: 100dvh;
        display: grid;
        place-items: center;
        padding: 1.25rem;
      }

      .page__inner {
        max-width: 720px;
        padding: 1.5rem;
        border-radius: 1rem;
        border: 1px solid #e5e7eb;
        background: #ffffff;
      }

      h1 {
        margin: 0;
      }

      p {
        margin: 0.75rem 0 1.25rem;
        color: #4b5563;
      }
    `
  ]
})
export class BookPageComponent implements OnInit {
  private readonly contentService = inject(LandingContentService);
  private readonly seoService = inject(SeoService);

  readonly bookingUrl = this.contentService.bookingUrl;

  ngOnInit(): void {
    this.seoService.setPageMeta(
      'Book a Call | AI Automation',
      'Schedule a discovery call for AI automation and agent workflow implementation.'
    );
  }
}
