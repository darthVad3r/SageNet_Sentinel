# Validator Agent — Handoffs
Version: 1.0.0

## Outbound Handoffs
### ? Compliance — escalate compliance failures
### ? Coordinator — return validation result (pass/fail) to pipeline
### ? Risk Officer — escalate drift or hallucination threshold breaches

## Inbound Handoffs
### ? Any agent — receives output for validation
### ? Coordinator — dispatches validation task
